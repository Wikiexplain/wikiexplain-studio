image: node:12.10.0
cache:
  paths:
    - node_modules/
    - .cache/
    - public/
pages:
  script:
    - yarn
    - yarn build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - master